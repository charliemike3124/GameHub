export default function Mode(name, url, desc, component, options){
    return {
        name: name,
        url: url,
        desc: desc,        
        component: component,
        options: options,
    }
}
